#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=- 1;DB_CLOSE_ON_EXIT=FALSE

spring.datasource.url= jdbc:mysql://localhost:3306/dbescolastico?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
#spring.jpa.hibernate.ddl-auto=create
spring.jpa.hibernate.ddl-auto=update

#Nuevos - Features
# 1. Administración de Clubes { nombre, descripcion, instructor } { Steve - Luis }
# 2. Administración de Giras { fecha inicio, fecha final, destino, coordinador, objetivo } { Bryan - Alejandro }
# 3. Administración de Profesores { Kevin - Xavier }
# 4. Administración de Aulas {código del aula}  { Cindy - Kassandra }
# 5. Administración de Areas { Carlos - Christopher }
# 6. Administración de Carreras {Javier - Jefferson}
# 7. Administración de Tipo de becas { nombre, monto x semestre, descripcion, nota minima } { Elvis }
# 8. Administración de Laboratorios { Tomas - Santiago }
# 9. Administración de Pry Vinculación {nombre, comunidad, responsable, fecha_i, fecha_fin, objetivo } {Juan Carlos - Dair} 
# 10. Administración de Tutoría { fecha, numbero horas, docente, materia } { Odalis - David}

# Escritura del requerimiento (x1)
# El uso del tablero (x1)
# El uso de git con branches (x2)
# La parte visual incorporada al template (x3)
# La funcionalidad Create (x3)
# La funcionalidad Retrieve (x2)
# La funcionalidad Update (x2)
# La funcionalidad Delete (x3) { El eliminar debe ser con el sweet alert }
# La funcionalidad List (x3)

